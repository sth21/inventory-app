<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Shirt Shop - <%= locals.category.name %></title>
</head>
<body>
    <h1><% locals.category.name %></h1>
    <a href="/">Go Back</a>
    <a href="<%= locals.category.url %>/new">Add New Shirt</a>
    <div class="shirt-wrapper">
        <% locals.shirts.forEach((shirt) => { %>
            <a href="<%= category.url %><%= shirt.url %>" class="shirt">
                <p><%= shirt.name %></p>
                <p><%= shirt.price %></p>
            </a>
        <% }) %>
    </div>
</body>
</html>